<Project>
  <PropertyGroup>
    <ManagePackageVersionsCentrally>true</ManagePackageVersionsCentrally>
  </PropertyGroup>
  <ItemGroup>
    <!-- Expression evaluation for calculator example -->
    <PackageVersion Include="Microsoft.CodeAnalysis.CSharp.Scripting" Version="5.0.0" />
    <!-- Unicode width calculation -->
    <PackageVersion Include="Wcwidth" Version="4.0.1" />
    <!-- Logging abstractions -->
    <PackageVersion Include="Microsoft.Extensions.Logging.Abstractions" Version="10.0.0-preview.1.25080.5" />
    <!-- Syntax highlighting via TextMate grammars (MIT) -->
    <PackageVersion Include="TextMateSharp" Version="1.0.70" />
    <PackageVersion Include="TextMateSharp.Grammars" Version="1.0.69" />
    <!-- SSH server library (MIT) -->
    <PackageVersion Include="FxSsh" Version="1.3.0" />
    <!-- SSH client for testing (MIT) -->
    <PackageVersion Include="SSH.NET" Version="2024.2.0" />
    <!-- SQLite database access (MIT) -->
    <PackageVersion Include="Microsoft.Data.Sqlite" Version="9.0.2" />
    <!-- Test packages (xUnit v3 with native dynamic skipping) -->
    <PackageVersion Include="Microsoft.NET.Test.Sdk" Version="17.12.0" />
    <PackageVersion Include="xunit.v3" Version="3.0.1" />
    <PackageVersion Include="xunit.runner.visualstudio" Version="3.0.1" />
    <PackageVersion Include="coverlet.collector" Version="6.0.2" />
    <!-- Benchmark packages -->
    <PackageVersion Include="BenchmarkDotNet" Version="0.15.8" />
  </ItemGroup>
</Project>